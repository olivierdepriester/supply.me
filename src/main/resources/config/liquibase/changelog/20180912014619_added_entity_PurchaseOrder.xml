<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <property name="now" value="now()" dbms="h2"/>

    <property name="now" value="current_timestamp" dbms="postgresql"/>

    <property name="floatType" value="float4" dbms="postgresql, h2"/>
    <property name="floatType" value="float" dbms="mysql, oracle, mssql"/>

    <!--
        Added the entity PurchaseOrder.
    -->
    <changeSet id="20180912014619-1" author="jhipster">
        <createTable tableName="purchase_order">
            <column name="id" type="bigint" autoIncrement="${autoIncrement}">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="code" type="varchar(10)">
                <constraints nullable="false" />
            </column>

            <column name="expected_date" type="datetime">
                <constraints nullable="true" />
            </column>

            <column name="status" type="varchar(255)">
                <constraints nullable="false" />
            </column>

            <column name="creation_date" type="datetime">
                <constraints nullable="false" />
            </column>

            <column name="supplier_id" type="bigint">
                <constraints nullable="true" />
            </column>

            <column name="creation_user_id" type="bigint">
                <constraints nullable="true" />
            </column>

            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here, do not remove-->
        </createTable>
        <dropDefaultValue tableName="purchase_order" columnName="expected_date" columnDataType="datetime"/>
        <dropDefaultValue tableName="purchase_order" columnName="creation_date" columnDataType="datetime"/>

    </changeSet>
    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here, do not remove-->
    <changeSet id="20181011120000-1" author="olivier">
        <addColumn tableName="purchase_order">
            <column name="quantity" type="double">
                <constraints nullable="true" />
            </column>
        </addColumn>
        <addColumn tableName="purchase_order">
            <column name="amount" type="double">
                <constraints nullable="true" />
            </column>
        </addColumn>
        <addColumn tableName="purchase_order">
            <column name="number_of_materials" type="bigint">
                <constraints nullable="true" />
            </column>
        </addColumn>
        <sql dbms="h2, postgresql"
            endDelimiter=";"
            splitStatements="true"
            stripComments="false">
            update purchase_order set quantity = t.quantity, amount = t.amount, number_of_materials = t.numberofmaterials
            from (
                select purchase_order_id,
                    sum(p.quantity) as quantity,
                    sum(p.quantity * p.order_price) as amount,
                    count(distinct d.material_id) as numberofmaterials
                from purchase_order_line p
                    inner join demand d on d.id = p.demand_id group by purchase_order_id
                ) t
            where t.purchase_order_id = purchase_order.id
        </sql>

    </changeSet>
</databaseChangeLog>
